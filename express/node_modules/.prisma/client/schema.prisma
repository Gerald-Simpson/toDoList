datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model toDoList {
  cookieId  String   @unique
  createdAt DateTime @default(now())
  message   String
}
